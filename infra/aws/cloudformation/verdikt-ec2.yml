AWSTemplateFormatVersion: "2010-09-09"
Description: Free-tier friendly Verdikt deployment on one EC2 instance.

Parameters:
  AppName:
    Type: String
    Default: verdikt
  ImageUri:
    Type: String
    Description: Full ECR image URI, for example 123456789012.dkr.ecr.us-east-1.amazonaws.com/verdikt:latest
  InstanceType:
    Type: String
    Default: t3.micro
    AllowedValues:
      - t2.micro
      - t3.micro
      - t4g.micro
    Description: Use a Free Tier eligible type for your account. Match the AMI architecture to the instance family.
  LatestAmiId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64
    Description: Amazon Linux 2023 AMI. Use the arm64 SSM parameter if you choose t4g.micro.
  AppPort:
    Type: Number
    Default: 8080
  AllowedCidr:
    Type: String
    Default: 127.0.0.1/32
    Description: Loopback-only by default. Set your public IP /32 for remote demos.
  ContainerMode:
    Type: String
    Default: real-mcp
    AllowedValues:
      - real-mcp
      - dashboard
    Description: Run the production MCP Streamable HTTP server or the dashboard.
  HttpBearerToken:
    Type: String
    NoEcho: true
    MinLength: 16
    Description: Required bearer token for the real MCP HTTP server.
  ApprovalSecret:
    Type: String
    NoEcho: true
    MinLength: 16
    Description: HMAC secret for Verdikt signed approval tokens.
  AuditHmacSecret:
    Type: String
    NoEcho: true
    MinLength: 16
    Description: Independent HMAC secret for tamper-evident audit events.

Resources:
  HttpBearerSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Description: Bearer token for remote MCP Streamable HTTP callers.
      SecretString: !Ref HttpBearerToken

  ApprovalSecretValue:
    Type: AWS::SecretsManager::Secret
    Properties:
      Description: HMAC secret for Verdikt signed approval tokens.
      SecretString: !Ref ApprovalSecret

  AuditHmacSecretValue:
    Type: AWS::SecretsManager::Secret
    Properties:
      Description: Independent HMAC secret for Verdikt audit evidence.
      SecretString: !Ref AuditHmacSecret

  InstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${AppName}-ec2-role-${AWS::Region}
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: ecr-pull
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - ecr:GetAuthorizationToken
                Resource: "*"
              - Effect: Allow
                Action:
                  - ecr:BatchCheckLayerAvailability
                  - ecr:BatchGetImage
                  - ecr:GetDownloadUrlForLayer
                Resource: !Sub arn:aws:ecr:${AWS::Region}:${AWS::AccountId}:repository/*
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Ref HttpBearerSecret
                  - !Ref ApprovalSecretValue
                  - !Ref AuditHmacSecretValue

  InstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - !Ref InstanceRole

  AppSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub Allow HTTP access to ${AppName}
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: !Ref AppPort
          ToPort: !Ref AppPort
          CidrIp: !Ref AllowedCidr
      Tags:
        - Key: Name
          Value: !Sub ${AppName}-sg

  AppInstance:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: !Ref LatestAmiId
      InstanceType: !Ref InstanceType
      IamInstanceProfile: !Ref InstanceProfile
      SecurityGroupIds:
        - !Ref AppSecurityGroup
      BlockDeviceMappings:
        - DeviceName: /dev/xvda
          Ebs:
            VolumeSize: 8
            VolumeType: gp3
            Encrypted: true
            DeleteOnTermination: true
      MetadataOptions:
        HttpEndpoint: enabled
        HttpTokens: required
      Tags:
        - Key: Name
          Value: !Ref AppName
        - Key: Project
          Value: Verdikt
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          set -euo pipefail

          dnf update -y
          dnf install -y docker awscli
          systemctl enable --now docker

          install -d -o 10001 -g 10001 /opt/verdikt/data
          aws ecr get-login-password --region ${AWS::Region} \
            | docker login --username AWS --password-stdin ${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com

          docker pull ${ImageUri}
          docker rm -f verdikt || true

          HTTP_BEARER_TOKEN="$(aws secretsmanager get-secret-value --region ${AWS::Region} --secret-id ${HttpBearerSecret} --query SecretString --output text)"
          APPROVAL_SECRET="$(aws secretsmanager get-secret-value --region ${AWS::Region} --secret-id ${ApprovalSecretValue} --query SecretString --output text)"
          AUDIT_HMAC_SECRET="$(aws secretsmanager get-secret-value --region ${AWS::Region} --secret-id ${AuditHmacSecretValue} --query SecretString --output text)"

          docker run -d \
            --name verdikt \
            --restart unless-stopped \
            -p ${AppPort}:8080 \
            -v /opt/verdikt/data:/app/data \
            -e VERDIKT_MODE=${ContainerMode} \
            -e VERDIKT_TELEMETRY=disabled \
            -e VERDIKT_HTTP_BEARER_TOKEN="$HTTP_BEARER_TOKEN" \
            -e VERDIKT_APPROVAL_SECRET="$APPROVAL_SECRET" \
            -e VERDIKT_AUDIT_HMAC_SECRET="$AUDIT_HMAC_SECRET" \
            -e VERDIKT_AUDIT_SIGNATURE_REQUIRED=true \
            -e VERDIKT_AUDIT_VERIFY_ON_STARTUP=true \
            ${ImageUri}

Outputs:
  InstanceId:
    Description: EC2 instance ID
    Value: !Ref AppInstance
  PublicDnsName:
    Description: Public DNS name
    Value: !GetAtt AppInstance.PublicDnsName
  DashboardUrl:
    Description: Verdikt service URL. In real-mcp mode, use /mcp for MCP clients and /healthz for health checks.
    Value: !Sub http://${AppInstance.PublicDnsName}:${AppPort}
  McpEndpointUrl:
    Description: Official MCP Streamable HTTP endpoint
    Value: !Sub http://${AppInstance.PublicDnsName}:${AppPort}/mcp
  SsmConnectCommand:
    Description: Session Manager command
    Value: !Sub aws ssm start-session --target ${AppInstance}
