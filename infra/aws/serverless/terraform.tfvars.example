aws_region      = "us-east-1"
app_name        = "gatetrace-serverless"
lambda_zip_path = "../../../build/gatetrace-serverless.zip"

# Replace both values before sharing a public API URL.
api_token       = "replace-me-with-a-long-random-token"
approval_secret = "replace-me-with-a-long-random-secret"
