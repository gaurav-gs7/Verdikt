{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp-guard.serviceAccountName" . }}
  labels:
    {{- include "mcp-guard.labels" . | nindent 4 }}
{{- end }}
