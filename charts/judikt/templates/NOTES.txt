Judikt is installed.

Port-forward the service:

  kubectl port-forward svc/{{ include "judikt.fullname" . }} 8080:{{ .Values.service.port }}

Health:

  curl http://127.0.0.1:8080/healthz

MCP endpoint:

  http://127.0.0.1:8080/mcp
