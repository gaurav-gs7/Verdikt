{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "judikt.serviceAccountName" . }}
  labels:
    {{- include "judikt.labels" . | nindent 4 }}
{{- end }}
