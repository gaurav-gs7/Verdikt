[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "verdikt"
version = "0.2.0"
description = "A deterministic security and reliability control plane for MCP tool execution."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Gaurav GS" }]
license = { text = "Apache-2.0" }

[project.scripts]
verdikt = "verdikt.cli:main"
# Compatibility aliases retained for pre-Verdikt local configurations.
gatetrace-mcp = "verdikt.cli:main"
mcp-guard = "verdikt.cli:main"

[project.optional-dependencies]
mcp = [
  "mcp>=1.28.1,<2",
  "pydantic-settings>=2.14.2,<3",
  "starlette>=1.3.1,<2",
  "uvicorn[standard]>=0.30,<1",
]
observability = [
  "openinference-semantic-conventions>=0.1.30,<0.2",
  "opentelemetry-api>=1.39,<2",
  "opentelemetry-sdk>=1.39,<2",
  "opentelemetry-exporter-otlp-proto-http>=1.39,<2",
]
auth = [
  "cryptography>=48.0.1,<50",
  "PyJWT[crypto]>=2.10,<3",
]
redis = [
  "redis>=5,<7",
]
benchmark = [
  "pyarrow>=18,<22",
]
aws = [
  "boto3>=1.34,<2",
]
test = [
  "coverage[toml]>=7.6,<8",
]
quality = [
  "pip-audit>=2.10,<3",
  "ruff>=0.12,<1",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
